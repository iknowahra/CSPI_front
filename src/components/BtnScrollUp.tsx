import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  border: 1px solid black;
`;

function BtnScrollUp() {
  return <Wrapper>BtnScrollUp</Wrapper>;
}

export default BtnScrollUp;
